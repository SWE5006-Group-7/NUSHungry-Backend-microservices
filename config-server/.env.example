# Config Server Git Repository Authentication
# If using private repository, uncomment and configure:

# GitHub Personal Access Token (classic with 'repo' scope)
# CONFIG_GIT_USERNAME=your-github-username
# CONFIG_GIT_PASSWORD=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Or use GitHub Deploy Key (recommended for production)
# Generate: ssh-keygen -t ed25519 -C "config-server-deploy-key"
# Add public key to GitHub repo: Settings → Deploy keys
